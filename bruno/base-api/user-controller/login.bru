meta {
  name: login
  type: http
  seq: 2
}

post {
  url: {{test-api-url}}/auth/login
  body: json
  auth: none
}

body:json {
  {
    "username":"test",
    "password":"12345"
  }
}

tests {
  test("should be able to login", function() {
    const data = res.getBody();
    expect(res.getStatus()).to.equal(200);
  });
   
}
